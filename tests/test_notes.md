The ```__init__.py``` file allows us to import the functions from the /src folder into the
/test folder, e.g., src.config

We can run the test files by doing the following:
```
cd "C:\Users\maxan\OneDrive\Desktop\0. Personal Projects\market-intelligence-pipeline"
python -m tests.test_ingest
```